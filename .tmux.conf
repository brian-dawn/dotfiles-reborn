unbind-key C-b
set -g prefix 'C-o'
bind-key 'C-o' send-prefix

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'Morantron/tmux-fingers'

run '~/.tmux/plugins/tpm/tpm'

# Fix large escape delay for vim.
set -sg escape-time 0

set-option -g mouse on
